enum PageType {
  PRODUCT = "product",
  CATEGORY = "category",
}

enum Conditions {
  BRAND_NEW = "brand new",
  LIKE_NEW = "like new",
  GENTLY_USED = "gently used",
  GOOD = "good",
  FAIR = "fair",
  FUNCTIONAL = "functional",
  NEEDS_ATTENTION = "needs attention",
}

enum DeliveryOptions {
  PICKUP = 1,
  DELIVERY = 2,
}

export { PageType, Conditions };
